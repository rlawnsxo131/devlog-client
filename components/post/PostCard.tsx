import * as React from 'react';
import styled from 'styled-components';

type PostCardProps = {};

function PostCard(props: PostCardProps) {
  return <Block>PostCard</Block>;
}

const Block = styled.div``;

export default PostCard;
